name: CI - build & test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend:
    name: Backend (Spring Boot + Selenium + H2)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      # 1️⃣ Checkout the code
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Set up Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # 3️⃣ Cache Maven dependencies
      - name: Cache Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      # 4️⃣ Install Google Chrome & ChromeDriver (required for Selenium UI tests)
      - name: Install Google Chrome & ChromeDriver
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable chromium-chromedriver

      # 🟢 Set up Node for frontend build
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # 🟢 Cache npm for frontend
      - name: Cache npm (frontend)
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-

      # 🟢 Build & serve frontend on 5173
      - name: Build & serve frontend on 5173
        working-directory: ./frontend
        run: |
          npm ci
          npm run build
          npx serve -s build -l 5173 &

      - name: Wait for frontend
        run: sleep 5

      # 5️⃣ Build and run tests (unit + Selenium)
      - name: Build & run tests (Maven)
        run: mvn -B clean verify

      # 6️⃣ Upload backend artifacts
      - name: Upload backend artifacts (jar + test reports)
        uses: actions/upload-artifact@v4
        with:
          name: backend-artifacts
          path: |
            target/*.jar
            target/surefire-reports/**/*.xml
            build/libs/*.jar

  frontend:
    name: Frontend (React)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      # 1️⃣ Checkout the code
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Set up Node.js (same as local)
      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # 3️⃣ Cache npm
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-

      # 4️⃣ Clean npm cache to avoid stale state
      - name: Clean npm cache
        run: npm cache clean --force

      # 5️⃣ Install dependencies (use npm ci for lockfile)
      - name: Install dependencies
        run: npm ci

      # 6️⃣ Run unit tests
      - name: Run unit tests
        run: npm test -- --watchAll=false

      # 7️⃣ Build frontend
      - name: Build frontend
        run: npm run build

      # 8️⃣ Upload frontend build
      - name: Upload frontend build
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: build

          
